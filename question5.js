//Use pop method to remove last element in the array, and log the removed element. Also log the length after removal of element.

let primitieDataType = ["String", "number", "boolean", "null", "undefined", "symbol", "bigint"];

let a=primitieDataType.pop();

console.log(a);