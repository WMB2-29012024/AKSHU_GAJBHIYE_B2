//Create an object with key “primitivetypes”, and value should be an array of strings containing names of all primitive data types in JS

let obj = {
    "primitivetypes": [
        "String", "number", "boolean", "null", "undefined", "symbol", "bigint"
    ]
}

console.log(obj);